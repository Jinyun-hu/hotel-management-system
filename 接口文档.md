# 酒店管理系统接口文档

**版本：** 1.1.0  
**更新时间：** 2026-03-31  
**接口基础路径：** `/api`

---

## 目录

1. [用户认证](#1-用户认证)
2. [数据统计](#2-数据统计)
3. [房型管理](#3-房型管理)
4. [房间管理](#4-房间管理)
5. [订单管理](#5-订单管理)
6. [房态可视化](#6-房态可视化)

---

## 1. 用户认证

### 1.1 用户登录

#### 1.1.1 基本信息

> 请求路径： `/api/auth/login`  
> 请求方式： `POST`  
> 接口描述： 用户登录验证接口，支持记住密码功能

#### 1.1.2 请求参数

参数格式： `application/json`

| 参数名     | 类型   | 是否必须 | 备注               |
| ---------- | ------ | -------- | ------------------ |
| username   | string | 必须     | 用户名             |
| password   | string | 必须     | 密码               |
| rememberMe | bool   | 非必须   | 是否记住登录状态   |

请求参数样例：

```json
{
  "username": "admin",
  "password": "123456",
  "rememberMe": true
}
```

#### 1.1.3 响应数据

参数格式： `application/json`

| 参数名   | 类型   | 是否必须 | 备注                           |
| -------- | ------ | -------- | ------------------------------ |
| code     | number | 必须     | 响应码，1 代表成功，0 代表失败 |
| msg      | string | 非必须   | 提示信息                       |
| data     | object | 非必须   | 返回的用户信息                 |
| \|- token | string | 非必须   | 登录令牌                       |
| \|- user  | object | 非必须   | 用户信息对象                   |

响应数据样例：

```json
{
  "code": 1,
  "msg": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "admin",
      "name": "管理员",
      "role": "admin"
    }
  }
}
```

---

### 1.2 用户注册

#### 1.2.1 基本信息

> 请求路径： `/api/auth/register`  
> 请求方式： `POST`  
> 接口描述： 新用户注册接口，注册成功后自动登录并返回JWT Token

#### 1.2.2 请求参数

参数格式： `application/json`

| 参数名         | 类型   | 是否必须 | 备注                                   |
| -------------- | ------ | -------- | -------------------------------------- |
| username       | string | 必须     | 用户名（3-20字符，字母/数字/下划线）   |
| password       | string | 必须     | 密码（6-20字符）                       |
| confirmPassword| string | 必须     | 确认密码，必须与password一致           |
| name           | string | 必须     | 真实姓名（最多50字符）                 |
| role           | string | 非必须   | 角色（默认为user）                     |

请求参数样例：

```json
{
  "username": "zhangsan",
  "password": "123456",
  "confirmPassword": "123456",
  "name": "张三",
  "role": "user"
}
```

#### 1.2.3 响应数据

参数格式： `application/json`

| 参数名   | 类型   | 是否必须 | 备注                           |
| -------- | ------ | -------- | ------------------------------ |
| code     | number | 必须     | 响应码，1 代表成功，0 代表失败 |
| msg      | string | 非必须   | 提示信息                       |
| data     | object | 非必须   | 返回的用户信息                 |
| \|- token | string | 非必须   | 登录令牌（24小时有效）        |
| \|- user  | object | 非必须   | 用户信息对象                   |

响应数据样例：

```json
{
  "code": 1,
  "msg": "注册成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 2,
      "username": "zhangsan",
      "name": "张三",
      "role": "user"
    }
  }
}
```

**⚠️ 重要说明**：
- 用户名必须唯一，重复会返回错误
- 密码使用BCrypt加密存储，响应中不返回密码字段
- 默认角色为 `user`，`admin` 角色需谨慎分配
- 注册成功后自动登录，返回JWT Token用于后续认证
- Token有效期为24小时，需在后续请求的Header中添加：`Authorization: Bearer {token}`

**常见错误**：
- 用户名已存在：`{"code": 1002, "message": "用户已存在"}`
- 两次密码不一致：`{"code": 400, "message": "两次输入的密码不一致"}`

---

### 1.3 用户退出

#### 1.2.1 基本信息

> 请求路径： `/api/auth/logout`  
> 请求方式： `POST`  
> 接口描述： 用户退出登录，清除登录状态

#### 1.2.2 请求参数

无

#### 1.2.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注                           |
| ------ | ------ | -------- | ------------------------------ |
| code   | number | 必须     | 响应码，1 代表成功，0 代表失败 |
| msg    | string | 非必须   | 提示信息                       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "退出成功"
}
```

---

### 1.4 获取当前用户信息

#### 1.4.1 基本信息

> 请求路径： `/api/auth/current`  
> 请求方式： `GET`  
> 接口描述： 获取当前登录用户的信息

#### 1.4.2 请求参数

无（需携带登录令牌）

#### 1.4.3 响应数据

参数格式： `application/json`

| 参数名  | 类型   | 是否必须 | 备注             |
| ------- | ------ | -------- | ---------------- |
| code    | number | 必须     | 响应码           |
| msg     | string | 非必须   | 提示信息         |
| data    | object | 非必须   | 用户信息对象     |
| \|- id  | number | 非必须   | 用户ID           |
| \|- username | string | 非必须 | 用户名       |
| \|- name | string | 非必须  | 姓名             |
| \|- role | string | 非必须  | 角色             |

响应数据样例：

```json
{
  "code": 1,
  "msg": "success",
  "data": {
    "id": 1,
    "username": "admin",
    "name": "管理员",
    "role": "admin"
  }
}
```

---



### 1.8 更新用户资料

#### 1.8.1 基本信息

> 请求路径： `/api/auth/update-profile`
> 请求方式： `PUT`
> 接口描述： 更新当前登录用户的资料（姓名）

#### 1.8.2 请求参数

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注                   |
| ------ | ------ | -------- | ---------------------- |
| name   | string | 非必须   | 姓名（最多50字符）     |

请求参数样例：

```json
{
  "name": "张三"
}
```

#### 1.8.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注                   |
| ------ | ------ | -------- | ---------------------- |
| code   | number | 必须     | 响应码                 |
| msg    | string | 非必须   | 提示信息               |
| data   | object | 非必须   | 更新后的用户信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "用户资料更新成功",
  "data": {
    "id": 1,
    "username": "zhangsan",
    "name": "张三",
    "role": "user"
  }
}
```

**⚠️ 重要说明**：
- 需要提供 name 字段
- 需要携带有效的JWT Token

---

## 2. 数据统计

### 2.1 获取运营统计数据

#### 2.1.1 基本信息

> 请求路径： `/api/statistics`  
> 请求方式： `GET`  
> 接口描述： 获取酒店运营统计数据，包括今日入住数、总房间数、可用房间数、今日订单数、总营收金额等

#### 2.1.2 请求参数

| 参数名     | 类型   | 是否必须 | 备注                   |
| ---------- | ------ | -------- | ---------------------- |
| dateRange  | number | 非必须   | 时间范围（天），默认7  |

#### 2.1.3 响应数据

参数格式： `application/json`

| 参数名              | 类型   | 是否必须 | 备注               |
| ------------------- | ------ | -------- | ------------------ |
| code                | number | 必须     | 响应码             |
| msg                 | string | 非必须   | 提示信息           |
| data                | object | 非必须   | 统计数据对象       |
| \|- todayCheckIn    | number | 非必须   | 今日入住数         |
| \|- todayCheckInRate | number | 非必须 | 较昨日变化百分比   |
| \|- totalRooms      | number | 非必须   | 总房间数           |
| \|- floorCount      | number | 非必须   | 楼层数             |
| \|- availableRooms  | number | 非必须   | 可用房间数         |
| \|- occupancyRate   | number | 非必须   | 入住率             |
| \|- todayOrders     | number | 非必须   | 今日订单数         |
| \|- orderGrowthRate | number | 非必须   | 订单增长率         |
| \|- totalRevenue    | number | 非必须   | 总营收金额         |
| \|- todayRevenue    | number | 非必须   | 今日收入           |

响应数据样例：

```json
{
  "code": 1,
  "msg": "success",
  "data": {
    "todayCheckIn": 24,
    "todayCheckInRate": 8,
    "totalRooms": 156,
    "floorCount": 8,
    "availableRooms": 87,
    "occupancyRate": 44,
    "todayOrders": 36,
    "orderGrowthRate": 12,
    "totalRevenue": 28650,
    "todayRevenue": 9240
  }
}
```

---

### 2.2 导出统计数据

#### 2.2.1 基本信息

> 请求路径： `/api/statistics/export`  
> 请求方式： `GET`  
> 接口描述： 导出酒店运营统计数据为Excel文件

#### 2.2.2 请求参数

| 参数名     | 类型   | 是否必须 | 备注                   |
| ---------- | ------ | -------- | ---------------------- |
| dateRange  | number | 非必须   | 时间范围（天），默认7  |

#### 2.2.3 响应数据

返回文件流（Content-Type: application/vnd.ms-excel）

---

## 3. 房型管理

### 3.1 房型列表查询

#### 3.1.1 基本信息

> 请求路径： `/api/room-types`  
> 请求方式： `GET`  
> 接口描述： 获取所有房型列表，支持搜索和筛选

#### 3.1.2 请求参数

| 参数名  | 类型   | 是否必须 | 备注               |
| ------- | ------ | -------- | ------------------ |
| keyword | string | 非必须   | 房型名称关键字     |
| status  | string | 非必须   | 房型状态           |

#### 3.1.3 响应数据

参数格式： `application/json`

| 参数名       | 类型      | 是否必须 | 备注               |
| ------------ | --------- | -------- | ------------------ |
| code         | number    | 必须     | 响应码             |
| msg          | string    | 非必须   | 提示信息           |
| data         | object[]  | 非必须   | 房型列表数据       |
| \|- id       | number    | 非必须   | 房型ID             |
| \|- name     | string    | 非必须   | 房型名称           |
| \|- capacity | number    | 非必须   | 可住人数           |
| \|- beds     | string    | 非必须   | 床位数量           |
| \|- price    | number    | 非必须   | 参考价格           |
| \|- status   | string    | 非必须   | 状态（active/inactive） |
| \|- image    | string    | 非必须   | 房型封面图片URL    |
| \|- createTime | string  | 非必须   | 创建时间           |
| \|- updateTime | string  | 非必须   | 更新时间           |

响应数据样例：

```json
{
  "code": 1,
  "msg": "success",
  "data": [
    {
      "id": 1,
      "name": "豪华大床房",
      "capacity": 2,
      "beds": "1张特大床",
      "price": 888,
      "status": "active",
      "image": "https://design.gemcoder.com/staticResource/echoAiSystemImages/3281e15c17ec45ef14401098032f7dc0.png",
      "createTime": "2024-01-01 10:00:00",
      "updateTime": "2024-01-01 10:00:00"
    },
    {
      "id": 2,
      "name": "标准双床房",
      "capacity": 2,
      "beds": "2张单人床",
      "price": 688,
      "status": "active",
      "image": "https://design.gemcoder.com/staticResource/echoAiSystemImages/2b0c2e26f1a9e8bee706e49c359916f8.png",
      "createTime": "2024-01-01 10:00:00",
      "updateTime": "2024-01-01 10:00:00"
    }
  ]
}
```

---

### 3.2 新增房型

#### 3.2.1 基本信息

> 请求路径： `/api/room-types`  
> 请求方式： `POST`  
> 接口描述： 添加新的房型

#### 3.2.2 请求参数

参数格式： `application/json`

| 参数名   | 类型   | 是否必须 | 备注           |
| -------- | ------ | -------- | -------------- |
| name     | string | 必须     | 房型名称       |
| capacity | number | 必须     | 可住人数       |
| beds     | string | 必须     | 床位数量       |
| price    | number | 必须     | 参考价格       |
| status   | string | 必须     | 状态           |
| image    | string | 非必须   | 房型封面图片   |

请求参数样例：

```json
{
  "name": "豪华大床房",
  "capacity": 2,
  "beds": "1张特大床",
  "price": 888,
  "status": "active",
  "image": "https://example.com/image.jpg"
}
```

#### 3.2.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |
| data   | object | 非必须   | 新增的房型对象 |

响应数据样例：

```json
{
  "code": 1,
  "msg": "房型添加成功",
  "data": {
    "id": 6,
    "name": "豪华大床房",
    "capacity": 2,
    "beds": "1张特大床",
    "price": 888,
    "status": "active",
    "image": "https://example.com/image.jpg",
    "createTime": "2024-01-15 10:00:00",
    "updateTime": "2024-01-15 10:00:00"
  }
}
```

---

### 3.3 编辑房型

#### 3.3.1 基本信息

> 请求路径： `/api/room-types/{id}`  
> 请求方式： `PUT`  
> 接口描述： 修改房型信息

#### 3.3.2 请求参数

参数格式： `application/json`

| 参数名   | 类型   | 是否必须 | 备注           |
| -------- | ------ | -------- | -------------- |
| name     | string | 非必须   | 房型名称       |
| capacity | number | 非必须   | 可住人数       |
| beds     | string | 非必须   | 床位数量       |
| price    | number | 非必须   | 参考价格       |
| status   | string | 非必须   | 状态           |
| image    | string | 非必须   | 房型封面图片   |

请求参数样例：

```json
{
  "name": "豪华大床房（升级版）",
  "price": 988,
  "status": "active"
}
```

#### 3.3.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "房型信息更新成功"
}
```

---

### 3.4 删除房型

#### 3.4.1 基本信息

> 请求路径： `/api/room-types/{id}`  
> 请求方式： `DELETE`  
> 接口描述： 删除指定房型

#### 3.4.2 请求参数

无

#### 3.4.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "房型删除成功"
}
```

---

## 4. 房间管理

### 4.1 房间列表查询

#### 4.1.1 基本信息

> 请求路径： `/api/rooms`  
> 请求方式： `GET`  
> 接口描述： 获取所有房间列表，支持搜索和筛选

#### 4.1.2 请求参数

| 参数名  | 类型   | 是否必须 | 备注                   |
| ------- | ------ | -------- | ---------------------- |
| keyword | string | 非必须   | 房间编号关键字         |
| type    | string | 非必须   | 所属房型               |
| status  | string | 非必须   | 房间状态               |

#### 4.1.3 响应数据

参数格式： `application/json`

| 参数名       | 类型   | 是否必须 | 备注                       |
| ------------ | ------ | -------- | -------------------------- |
| code         | number | 必须     | 响应码                     |
| msg          | string | 非必须   | 提示信息                   |
| data         | object[] | 非必须 | 房间列表数据               |
| \|- id       | number | 非必须   | 房间ID                     |
| \|- type     | string | 非必须   | 所属房型                   |
| \|- floor    | number | 非必须   | 楼层                       |
| \|- status   | string | 非必须   | 房间状态（occupied/available/cleaning/maintenance） |
| \|- cleanStatus | string | 非必须 | 清洁状态（cleaned/dirty/cleaning） |
| \|- price    | number | 非必须   | 价格                       |
| \|- createTime | string | 非必须 | 创建时间                   |
| \|- updateTime | string | 非必须 | 更新时间                   |

响应数据样例：

```json
{
  "code": 1,
  "msg": "success",
  "data": [
    {
      "id": 1001,
      "type": "标准间",
      "floor": 1,
      "status": "occupied",
      "cleanStatus": "cleaned",
      "price": 399,
      "createTime": "2024-01-01 10:00:00",
      "updateTime": "2024-01-01 10:00:00"
    },
    {
      "id": 1002,
      "type": "标准间",
      "floor": 1,
      "status": "available",
      "cleanStatus": "cleaned",
      "price": 399,
      "createTime": "2024-01-01 10:00:00",
      "updateTime": "2024-01-01 10:00:00"
    }
  ]
}
```

---

### 4.2 新增房间

#### 4.2.1 基本信息

> 请求路径： `/api/rooms`  
> 请求方式： `POST`  
> 接口描述： 添加新的房间

#### 4.2.2 请求参数

参数格式： `application/json`

| 参数名      | 类型   | 是否必须 | 备注           |
| ----------- | ------ | -------- | -------------- |
| type        | string | 必须     | 所属房型       |
| floor       | number | 必须     | 楼层           |
| status      | string | 必须     | 房间状态       |
| cleanStatus | string | 必须     | 清洁状态       |
| price       | number | 必须     | 价格           |

请求参数样例：

```json
{
  "type": "标准间",
  "floor": 1,
  "status": "available",
  "cleanStatus": "cleaned",
  "price": 399
}
```

#### 4.2.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |
| data   | object | 非必须   | 新增的房间对象 |

响应数据样例：

```json
{
  "code": 1,
  "msg": "房间添加成功",
  "data": {
    "id": 1005,
    "type": "标准间",
    "floor": 1,
    "status": "available",
    "cleanStatus": "cleaned",
    "price": 399,
    "createTime": "2024-01-15 10:00:00",
    "updateTime": "2024-01-15 10:00:00"
  }
}
```

---

### 4.3 编辑房间

#### 4.3.1 基本信息

> 请求路径： `/api/rooms/{id}`  
> 请求方式： `PUT`  
> 接口描述： 修改房间信息

#### 4.3.2 请求参数

**路径参数**：
| 参数名 | 类型   | 是否必须 | 备注                   |
| ------ | ------ | -------- | ---------------------- |
| id     | number | 必须     | 房间ID（在URL路径中） |

**请求体**（application/json）：
| 参数名        | 类型   | 是否必须 | 备注                                   |
| ------------- | ------ | -------- | -------------------------------------- |
| roomNumber    | string | 非必须   | 房间编号                               |
| roomTypeId    | number | 非必须   | 房型ID                                 |
| floor         | number | 非必须   | 楼层（1-100）                         |
| status        | string | 非必须   | 房间状态                               |
| cleanStatus   | string | 非必须   | 清洁状态                               |
| price         | number | 非必须   | 价格（Decimal格式）                    |

请求参数样例：

```json
{
  "roomNumber": "1001",
  "roomTypeId": 2,
  "floor": 1,
  "status": "occupied",
  "cleanStatus": "dirty",
  "price": 1288.00
}
```

#### 4.3.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "房间信息更新成功"
}
```

**⚠️ 重要提示**：
- ✅ 正确: `PUT /api/rooms/1` （URL中必须包含房间ID）
- ❌ 错误: `PUT /api/rooms/` （缺少ID会返回404）

---

### 4.4 删除房间

#### 4.4.1 基本信息

> 请求路径： `/api/rooms/{id}`  
> 请求方式： `DELETE`  
> 接口描述： 删除指定房间

#### 4.4.2 请求参数

无

#### 4.4.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "房间删除成功"
}
```

---

## 5. 订单管理

### 5.1 订单列表查询

#### 5.1.1 基本信息

> 请求路径： `/api/orders`  
> 请求方式： `GET`  
> 接口描述： 获取所有订单列表，支持搜索和筛选

#### 5.1.2 请求参数

| 参数名   | 类型   | 是否必须 | 备注                   |
| -------- | ------ | -------- | ---------------------- |
| orderId  | string | 非必须   | 订单编号关键字         |
| guest    | string | 非必须   | 客人姓名               |
| phone    | string | 非必须   | 客人电话               |
| roomType | string | 非必须   | 房型名称               |
| status   | string | 非必须   | 订单状态               |
| payment  | string | 非必须   | 支付状态               |

#### 5.1.3 响应数据

参数格式： `application/json`

| 参数名        | 类型   | 是否必须 | 备注                       |
| ------------- | ------ | -------- | -------------------------- |
| code          | number | 必须     | 响应码                     |
| msg           | string | 非必须   | 提示信息                   |
| data          | object[] | 非必须 | 订单列表数据               |
| \|- id        | string | 非必须   | 订单编号                   |
| \|- guest     | string | 非必须   | 客人姓名                   |
| \|- phone     | string | 非必须   | 客人电话                   |
| \|- roomType  | string | 非必须   | 房型名称                   |
| \|- room      | number | 非必须   | 房间号                     |
| \|- checkIn   | string | 非必须   | 入住日期                   |
| \|- checkOut  | string | 非必须   | 退房日期                   |
| \|- status    | string | 非必须   | 订单状态（active/completed/pending/canceled） |
| \|- payment   | string | 非必须   | 支付状态（paid/unpaid/refunded） |
| \|- totalAmount | number | 非必须 | 总金额                     |
| \|- createTime | string | 非必须 | 创建时间                   |
| \|- updateTime | string | 非必须 | 更新时间                   |

响应数据样例：

```json
{
  "code": 1,
  "msg": "success",
  "data": [
    {
      "id": "ORD20230615001",
      "guest": "张三",
      "phone": "13800138000",
      "roomType": "豪华大床房",
      "room": 801,
      "checkIn": "2023-06-15",
      "checkOut": "2023-06-18",
      "status": "active",
      "payment": "paid",
      "totalAmount": 2664,
      "createTime": "2024-01-01 10:00:00",
      "updateTime": "2024-01-01 10:00:00"
    }
  ]
}
```

---

### 5.2 新增订单

#### 5.2.1 基本信息

> 请求路径： `/api/orders`  
> 请求方式： `POST`  
> 接口描述： 创建新的订单

#### 5.2.2 请求参数

参数格式： `application/json`

| 参数名     | 类型   | 是否必须 | 备注           |
| ---------- | ------ | -------- | -------------- |
| guest      | string | 必须     | 客人姓名       |
| phone      | string | 必须     | 客人电话       |
| roomType   | string | 必须     | 房型名称       |
| room       | number | 必须     | 房间号         |
| checkIn    | string | 必须     | 入住日期       |
| checkOut   | string | 必须     | 退房日期       |
| totalAmount | number | 必须     | 总金额         |

请求参数样例：

```json
{
  "guest": "张三",
  "phone": "13800138000",
  "roomType": "豪华大床房",
  "room": 801,
  "checkIn": "2024-01-15",
  "checkOut": "2024-01-18",
  "totalAmount": 2664
}
```

#### 5.2.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |
| data   | object | 非必须   | 新增的订单对象 |

响应数据样例：

```json
{
  "code": 1,
  "msg": "订单创建成功",
  "data": {
    "id": "ORD20240115001",
    "guest": "张三",
    "phone": "13800138000",
    "roomType": "豪华大床房",
    "room": 801,
    "checkIn": "2024-01-15",
    "checkOut": "2024-01-18",
    "status": "pending",
    "payment": "unpaid",
    "totalAmount": 2664,
    "createTime": "2024-01-15 10:00:00",
    "updateTime": "2024-01-15 10:00:00"
  }
}
```

---

### 5.3 编辑订单

#### 5.3.1 基本信息

> 请求路径： `/api/orders/{id}`  
> 请求方式： `PUT`  
> 接口描述： 修改订单信息

#### 5.3.2 请求参数

参数格式： `application/json`

| 参数名     | 类型   | 是否必须 | 备注           |
| ---------- | ------ | -------- | -------------- |
| guest      | string | 非必须   | 客人姓名       |
| phone      | string | 非必须   | 客人电话       |
| room       | number | 非必须   | 房间号         |
| checkIn    | string | 非必须   | 入住日期       |
| checkOut   | string | 非必须   | 退房日期       |
| status     | string | 非必须   | 订单状态       |
| payment    | string | 非必须   | 支付状态       |

请求参数样例：

```json
{
  "status": "active",
  "payment": "paid"
}
```

#### 5.3.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "订单更新成功"
}
```

---

### 5.4 删除订单

#### 5.4.1 基本信息

> 请求路径： `/api/orders/{id}`  
> 请求方式： `DELETE`  
> 接口描述： 删除指定订单

#### 5.4.2 请求参数

无

#### 5.4.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "订单删除成功"
}
```

---

### 5.5 取消订单

#### 5.5.1 基本信息

> 请求路径： `/api/orders/{id}/cancel`  
> 请求方式： `POST`  
> 接口描述： 取消指定订单

#### 5.5.2 请求参数

无

#### 5.5.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "订单已取消"
}
```

---

## 6. 房态可视化

### 6.1 获取房态数据

#### 6.1.1 基本信息

> 请求路径： `/api/room-status`  
> 请求方式： `GET`  
> 接口描述： 获取所有房间的实时状态，用于可视化展示

#### 6.1.2 请求参数

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| floor  | number | 非必须   | 楼层筛选       |

#### 6.1.3 响应数据

参数格式： `application/json`

| 参数名       | 类型      | 是否必须 | 备注                       |
| ------------ | --------- | -------- | -------------------------- |
| code         | number    | 必须     | 响应码                     |
| msg          | string    | 非必须   | 提示信息                   |
| data         | object    | 非必须   | 房态数据对象               |
| \|- floors   | number[]  | 非必须   | 楼层列表                   |
| \|- rooms    | object[]  | 非必须   | 房间状态列表               |
| \|- id       | number    | 非必须   | 房间ID                     |
| \|- number   | string    | 非必须   | 房间编号                   |
| \|- floor    | number    | 非必须   | 楼层                       |
| \|- type     | string    | 非必须   | 房型                       |
| \|- status   | string    | 非必须   | 房间状态                   |
| \|- cleanStatus | string | 非必须   | 清洁状态               |
| \|- price    | number    | 非必须   | 价格                       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "success",
  "data": {
    "floors": [1, 2, 3, 4],
    "rooms": [
      {
        "id": 1001,
        "number": "1001",
        "floor": 1,
        "type": "标准间",
        "status": "occupied",
        "cleanStatus": "cleaned",
        "price": 399
      },
      {
        "id": 1002,
        "number": "1002",
        "floor": 1,
        "type": "标准间",
        "status": "available",
        "cleanStatus": "cleaned",
        "price": 399
      }
    ]
  }
}
```

---

### 6.2 更新房间状态

#### 6.2.1 基本信息

> 请求路径： `/api/room-status/{id}`  
> 请求方式： `PUT`  
> 接口描述： 快速更新房间状态（入住、退房、清洁、维修等）

#### 6.2.2 请求参数

参数格式： `application/json`

| 参数名      | 类型   | 是否必须 | 备注                     |
| ----------- | ------ | -------- | ------------------------ |
| status      | string | 必须     | 房间状态                 |
| cleanStatus | string | 非必须   | 清洁状态                 |

请求参数样例：

```json
{
  "status": "occupied",
  "cleanStatus": "dirty"
}
```

#### 6.2.3 响应数据

参数格式： `application/json`

| 参数名 | 类型   | 是否必须 | 备注           |
| ------ | ------ | -------- | -------------- |
| code   | number | 必须     | 响应码         |
| msg    | string | 非必须   | 提示信息       |

响应数据样例：

```json
{
  "code": 1,
  "msg": "房间状态更新成功"
}
```

---

## 附录

### 响应码说明

| 响应码 | 说明               |
| ------ | ------------------ |
| 1     | 请求成功           |
| 0     | 请求失败           |
| 200   | HTTP请求成功       |
| 400   | 请求参数错误       |
| 401   | 未授权（需登录）   |
| 403   | 无权限访问         |
| 404   | 资源不存在         |
| 500   | 服务器内部错误     |

### 业务错误码

| 错误码 | 说明                   |
| ------ | ---------------------- |
| 1002   | 用户已存在             |
| 1003   | 用户不存在             |
| 1004   | 用户已被禁用           |
| 1005   | 用户名或密码错误       |
| 1008   | 两次密码不一致         |

### 状态枚举

#### 房间状态 (status)
| 值          | 说明   |
| ----------- | ------ |
| available   | 空闲   |
| occupied    | 已入住 |
| cleaning    | 清洁中 |
| maintenance | 维修中 |

#### 清洁状态 (cleanStatus)
| 值        | 说明   |
| --------- | ------ |
| cleaned   | 已清洁 |
| dirty     | 需清洁 |
| cleaning  | 清洁中 |

#### 订单状态 (status)
| 值        | 说明     |
| --------- | -------- |
| pending   | 待入住   |
| active   | 入住中   |
| completed | 已完成  |
| canceled  | 已取消   |

#### 支付状态 (payment)
| 值       | 说明   |
| -------- | ------ |
| unpaid   | 未支付 |
| paid     | 已支付 |
| refunded | 已退款 |

#### 房型状态 (status)
| 值      | 说明   |
| ------- | ------ |
| active  | 启用   |
| inactive | 禁用  |

---

## 常见问题

### Q1: 如何正确调用PUT/DELETE接口？

A: PUT和DELETE接口需要在URL路径中包含资源ID，例如：
- ✅ 正确: `PUT /api/rooms/1` 或 `DELETE /api/rooms/1`
- ❌ 错误: `PUT /api/rooms/` 或 `DELETE /api/rooms/` （缺少ID会返回404）

### Q2: 房间管理接口的参数验证规则是什么？

A: 主要验证规则：
- `roomNumber`: 必须唯一，不能重复
- `roomTypeId`: 必须在room_type表中存在
- `floor`: 必须在1-100之间
- `status`: 必须是 available/occupied/cleaning/maintenance 之一
- `cleanStatus`: 必须是 cleaned/dirty/cleaning 之一
- `price`: 必须使用Decimal格式，如 888.00

### Q3: 用户注册后如何使用Token？

A: 注册成功后会返回JWT Token（24小时有效），请将Token保存到localStorage中，并在后续请求的Header中添加：
```
Authorization: Bearer {token}
```

### Q4: 如何在Swagger中测试API？

A: 访问 `http://localhost:8080/swagger-ui.html` 可以：
- 查看所有API端点的详细信息
- 在线测试API（自动生成正确的URL和请求格式）
- 查看请求/响应的Schema定义
- 直接复制curl命令

### Q5: 房间状态枚举值的含义是什么？

A:
- `available`: 空闲，可以预订
- `occupied`: 已入住
- `cleaning`: 清洁中
- `maintenance`: 维修中



### Q8: 如何更新用户资料？

A: 调用 `PUT /api/auth/update-profile` 接口，需要：
- 携带有效的JWT Token
- 需要提供 name 字段

---

## 测试检查清单

在进行API测试前，请确认：

- [ ] Spring Boot应用正在运行（http://localhost:8080）
- [ ] 数据库连接正常
- [ ] room_type表中有数据（房间创建需要）
- [ ] URL路径格式正确（PUT和DELETE需要包含ID）
- [ ] 请求头包含 `Content-Type: application/json`
- [ ] JSON数据格式正确，符合字段定义
- [ ] status和cleanStatus使用正确的枚举值
- [ ] roomTypeId存在于数据库中
- [ ] 价格使用BigDecimal格式（如888.00）
- [ ] 楼层在1-100之间

---

**文档版本历史**：
- v1.1.0 (2026-03-31): 整合房间管理API测试文档和用户注册API文档，更新字段验证规则和错误说明
- v1.0.0 (2024-01-15): 初始版本

**文档结束**
